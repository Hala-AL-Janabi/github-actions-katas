name: hello-world
on: push
jobs:
  my-job:
    runs-on: ci/build-app.sh
    container: gradle:6-jdk11
    steps:
      - uses: actions/checkout@v2
      - name: my-step
        run: echo "Hello World!"
